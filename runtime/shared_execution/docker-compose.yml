version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env.example
  frontend:
    image: python:3.12-slim
    working_dir: /app
    command: python -m http.server 4173
    volumes:
      - ./frontend:/app
    ports:
      - "4173:4173"

