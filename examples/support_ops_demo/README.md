# Support Ops Demo

This example shows that the runtime thesis is broader than coding agents while still following the same mechanics.

Suggested sequence:

1. Run `NL duplicate scenario` to see semantic overlap collapse into one shared execution unit.
2. Run `Unique-task counterexample` to see false-collapse safety keep materially different tasks separate.
3. Compare the collapse reasons and subscriber lists in the UI.
